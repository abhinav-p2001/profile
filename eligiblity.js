let age = 65; // You can change this value to test with different ages

let result = (age <= 0) ? "Invalid Age" :
             (age >= 60) ? "Eligible for Senior Discount" :
             "Not Eligible for Senior Discount";

console.log(result);
