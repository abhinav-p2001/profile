function printSlashesPattern(rows) {
    let pattern = "";
    for (let i = 0; i < rows; i++) {
      pattern += "\\/"; // Add \/ for each row
    }
    console.log(pattern);
  }
  
  // Example usage:
  printSlashesPattern(2);
  
  
  